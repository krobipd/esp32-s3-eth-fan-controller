# Session-/Tool-Cruft (interne Arbeitsdateien, nicht öffentlich)
.superpowers/
.remember/
.claude/
CLAUDE.md
docs/flash-checklist.md
docs/audit-*.md
docs/superpowers/
.DS_Store

# Datenblätter / Bilder (Copyright, groß)
*.pdf
*.jpg

# Arduino-Build-Artefakte (werden lokal/CI erzeugt).
# dist/ als GANZES, nicht nur nach Endung: --output-dir legt dort alles ab, was die Toolchain
# erzeugt, und eine Endungsliste laesst jede neue Artefaktart durch (eine SHA256SUMS.txt tat
# das prompt). Fertige Abbilder haengen an den GitHub-Releases, nicht im Repo.
build/
dist/
*.map
*.elf
*.bin
